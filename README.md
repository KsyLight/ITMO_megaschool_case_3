# Multi-Agent Interview Coach

[![Python 3.11](https://img.shields.io/badge/python-3.11-blue.svg)](https://www.python.org/)
[![LangGraph](https://img.shields.io/badge/AI-LangGraph-orange.svg)](https://python.langchain.com/docs/langgraph)
[![Streamlit](https://img.shields.io/badge/UI-Streamlit-red.svg)](https://streamlit.io/)
[![Status](https://img.shields.io/badge/Status-MVP-yellow.svg)]()

Интеллектуальная система для проведения технических собеседований (Mock Interview) с использованием мульти-агентной архитектуры. Проект имитирует реального рекрутера, проверяет знания кандидата и предоставляет детальный фидбэк.

## Текущий статус и ограничения

> **Важно:** Проект разработан в рамках Мегашколы ИТМО | 3 кейс
> 
> На данный момент **полноценно поддерживается и протестирован только GigaChat**. 
> Архитектура проекта (`llm.py`) заложена под мульти-провайдерность (OpenAI, Gemini), но адаптеры и промпты под эти модели **не реализованы до конца**. При переключении на других провайдеров возможна нестабильная работа парсера.

## Описание

**Interview Coach** — это приложение, построенное на базе **LangGraph**, где за разные этапы интервью отвечают специализированные AI-агенты. Система поддерживает работу через консоль (CLI) и через веб-интерфейс (Streamlit).

### Ключевые возможности:
- **Мульти-агентное ядро:** Разделение ролей (Intake, Interviewer, Fact-Checker, Reporter).
- **Web UI:** Удобный чат-интерфейс на Streamlit.
- **Smart Stop:** Умное распознавание команд завершения (например, "Стоп игра", "Давай фидбэк") с мгновенной генерацией отчета.
- **Устойчивость:** Система продолжает работу даже при сбоях форматирования ответов LLM.
- **Логирование по ТЗ:** Сохранение истории диалога в JSON с разделением на видимые сообщения и внутренние мысли агентов.

## Структура проекта

```text
.
├── app.py                  # Точка входа для CLI версии
├── ui.py                   # Точка входа для Web версии (Streamlit)
├── graph.py                # Архитектура графа (LangGraph) и маршрутизация
├── llm.py                  # Настройка LLM (Заточено под GigaChat)
├── logger.py               # Система логирования и форматирования JSON
├── utils.py                # Вспомогательные функции и "умная" проверка стоп-слов
├── .env                    # Переменные окружения (API ключи)
│
├── agents/                 # Пакет агентов
│   ├── intake.py           # Анализ профиля и стека кандидата
│   ├── interviewer.py      # Генерация вопросов и ведение диалога
│   ├── factchecker.py      # Проверка фактов и галлюцинаций
│   ├── reporter.py         # Генерация финального отчета
│   └── schemas.py          # Pydantic схемы данных (валидация)
│
└── outputs/                # Директория для сохраненных логов
    └── interview_log_*.json

```

## Быстрый старт

### 1. Требования

* Python 3.11
* Аккаунт разработчика **GigaChat** (обязательно).

### 2. Установка зависимостей

```bash
pip install -r requirements.txt

```

*Если файла `requirements.txt` нет:*

```bash
pip install python-dotenv langchain-core langgraph langchain-gigachat streamlit pydantic

```

### 3. Конфигурация (.env)

Создайте файл `.env` в корне проекта. Для стабильной работы используйте конфигурацию GigaChat:

```env
# --- ОСНОВНАЯ КОНФИГУРАЦИЯ (GigaChat) ---
LLM_PROVIDER=gigachat
LLM_MODEL=GigaChat-Pro
LLM_TEMPERATURE=0.2

# Учетные данные GigaChat API
GIGACHAT_CREDENTIALS=your_auth_data_here
GIGACHAT_SCOPE=GIGACHAT_API_PERS

# --- ЭКСПЕРИМЕНТАЛЬНО (Не завершено) ---
# OPENAI_API_KEY=...
# GOOGLE_API_KEY=...

```

### 4. Запуск

#### Вариант А: Веб-интерфейс (Рекомендуется)

```bash
streamlit run ui.py

```

Откройте браузер по адресу `http://localhost:8501`.

#### Вариант Б: Консольный режим

```bash
python app.py

```

## Сценарий использования

1. **Вход:** Вы вводите краткое описание (например: *"Привет, я Java Junior без опыта"*).
2. **Intake:** Агент анализирует ввод, определяет грейд и стек технологий.
3. **Interview:**
   * Агент задает вопросы по вашему стеку.
   * FactChecker "за кадром" проверяет ваши ответы на ложь или ошибки.

4. **Завершение:** В любой момент напишите **"Стоп"**, **"Завершить"** или **"Давай фидбэк"**.
5. **Отчет:** Система генерирует подробный JSON-лог и текстовый отчет с рекомендациями и выводом по кандидату.

## Архитектура (LangGraph)

Система работает как конечный автомат (State Graph):

1. **Node: Intake** — Инициализация профиля.
2. **Node: FactChecker** — Анализ ввода пользователя.
3. **Conditional Edge (Router):**
   * Если обнаружена команда "Стоп" -> Переход к **Reporter**.
   * Иначе -> Переход к **Interviewer**.

4. **Node: Interviewer** — Генерация следующего вопроса.
5. **Node: Reporter** — Сборка финального отчета.

### Формат логов

Логи сохраняются в папку `outputs/` в специальном формате:

```json
{
  "participant_name": "Алекс",
  "turns": [
    {
      "turn_id": 1,
      "user_message": "Знаю Python и SQL",
      "internal_thoughts": "[Intake]: Профиль определен. [Interviewer]: Нужно спросить про JOIN.",
      "agent_visible_message": "Отлично. Расскажи, какие типы JOIN ты знаешь?"
    }
  ],
  "final_feedback": "..."
}

```

*Конкретные примеры логов, которые были выполнены прогоном можно посмотреть в outputs, однако стоит учесть, что прогон осуществлялся на моделях разного уровня.*

## Решение проблем

**Проблема:** Ошибки при использовании OpenAI/Gemini.
**Решение:** Переключитесь на GigaChat в `.env`. Поддержка других моделей находится в стадии разработки (TODO).

## 👥 Авторство

Проект разработан в рамках **Мегашколы ИТМО | Кейс 3**.
